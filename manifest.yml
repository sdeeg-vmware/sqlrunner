---
defaults: &defaults
#  buildpacks:
#    - java_buildpack_offline
#  stack: cflinuxfs3
  instances: 1

applications:
  - name: sqlrunner
    <<: *defaults
    path: ./build/libs/sqlrunner-0.0.1-SNAPSHOT.jar
#    services:
#      - mysql-test-db
#      - pg-test-db
